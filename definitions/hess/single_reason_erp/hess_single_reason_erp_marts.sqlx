config {
  type: "table",
  schema: "gal_hess_marts",
  name: "hess_single_reason_erp_marts"
}

SELECT *
FROM  ${ref("Exchange_charge_single_reason_erp_history")} 
WHERE is_current = TRUE

UNION ALL

SELECT *
FROM  ${ref("Exchange_charge_single_reason_erp_line_2_history")} 
WHERE is_current = TRUE
