config {
  type: "table",
  schema: "gal_hess_marts",
  name: "hess_layer_marts"
}

SELECT *
FROM  ${ref("exchange_layer_history")} 
WHERE is_current = TRUE

UNION ALL

SELECT *
FROM  ${ref("exchange_layer_line_2_history")} 
WHERE is_current = TRUE
