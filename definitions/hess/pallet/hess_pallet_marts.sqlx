config {
  type: "table",
  schema: "gal_hess_marts",
  name: "hess_pallet_marts"
}

SELECT *
FROM  ${ref("Exchange_pallet_history")} 
WHERE is_current = TRUE

UNION ALL

SELECT *
FROM ${ref("Exchange_pallet_line_2_history")} 
WHERE is_current = TRUE
