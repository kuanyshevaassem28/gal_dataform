config {
  type: "table",
  schema: "gal_hess_marts",
  name: "hess_job_marts"
}

SELECT *
FROM  ${ref("Exchange_job_history")} 
WHERE is_current = TRUE

UNION ALL

SELECT *
FROM  ${ref("Exchange_job_line_2_history")} 
WHERE is_current = TRUE
