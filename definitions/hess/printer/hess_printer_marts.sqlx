config {
  type: "table",
  schema: "gal_hess_marts",
  name: "hess_printer_marts"
}

SELECT *
FROM  ${ref("Exchange_printer_history")} 
WHERE is_current = TRUE

UNION ALL

SELECT *
FROM  ${ref("Exchange_printer_line_2_history")} 
WHERE is_current = TRUE
